[project]
name = "secom-poc"
version = "0.1.0"
description = "Time-validated SECOM semiconductor failure detection + RCA PoC"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 88
target-version = "py311"
